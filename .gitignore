# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# the tool's own output — never commit a user's scanned inventory
stack-cleaner.json
/*-inventory.json

# original private source preserved locally — never published
/.archive/

# graphify knowledge-graph output (local build artifact, not source)
/graphify-out/

# subagent-driven-development scratch
.superpowers/

# --- private: machine/personal/process — NEVER publish, kept locally only ---
# Internal working docs: machine paths, npm account, full process history, the real
# scanned inventory, and Claude Code project notes. Stay on disk; never tracked.
/CLAUDE.md
/docs/_private/
/docs/SESSION-STATE.md
/docs/BUILD-LOG.md
/docs/HANDOFF.md
/docs/SESSION-LOG.md
/docs/worklog/
/docs/superpowers/
# docs/reference/example-scan-output.json is a SYNTHETIC public sample (tracked);
# the REAL scan lives in docs/_private/ and is never committed.
